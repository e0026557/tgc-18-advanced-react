import React from 'react';
// Alternatively:
// import {createContext} from 'react'

// Convention is to put the context through the provider
const StudentContext = React.createContext({});

export default StudentContext;
